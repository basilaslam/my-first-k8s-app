apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: LoadBalancer # k3s will handle this automatically!
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
